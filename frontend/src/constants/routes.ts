/** Map inside tenant portal (sidebar, no public Sign In bar). */
export const PORTAL_MAP_PATH = '/portal/map';

/** Map inside admin back-office (keeps sidebar & topbar). */
export const ADMIN_MAP_PATH = '/admin/map';

/** Public guest map. */
export const PUBLIC_MAP_PATH = '/map';
