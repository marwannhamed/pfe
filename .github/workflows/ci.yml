name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  backend:
    name: Backend - Lint, Test, Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --if-present

      - name: Test
        run: npm run test:cov --if-present

      - name: Build
        run: npm run build --if-present

  frontend:
    name: Frontend - Lint, Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --if-present

      # tsconfig.json is a solution file ("files": [] + references), so
      # `tsc --noEmit` type-checks nothing and always passes. `tsc -b` walks
      # the referenced projects and actually checks the source.
      - name: Type check
        run: npx tsc -b --force

      - name: Test coverage
        run: npm run test:coverage --if-present

      - name: Build
        run: npm run build --if-present

  security-audit:
    name: Security - Dependency Audit
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project:
          - backend
          - frontend
    defaults:
      run:
        working-directory: ${{ matrix.project }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ${{ matrix.project }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      # Reports but does not block. The frontend is clean at this level; the
      # backend still carries ~48 high advisories that can only be cleared by
      # major upgrades (@nestjs/* 10 -> 12, nodemailer 8 -> 10), which is a
      # migration in its own right. Drop continue-on-error once that is done
      # so regressions fail the build again.
      - name: Run npm audit (high/critical)
        run: npm audit --audit-level=high
        continue-on-error: true
